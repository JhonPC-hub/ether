services:
  - type: web
    name: ether
    env: node
    buildCommand: npm install && npm run build
    startCommand: npx serve -s build -l 10000
    envVars:
      - key: NODE_VERSION
        value: 18.x.0

